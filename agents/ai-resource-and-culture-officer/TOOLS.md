# Tools

- Instruction-bundle pilot workflow: use one managed bundle at a time, snapshot the full bundle before edits, keep only the latest 3 backups, run 1 canary task, then 3 to 5 directional comparison runs before preferring a new version.
- Canonical protocol: `doc/instruction-versioning-pilot.md`.
